---
version: "3.8"

services:
  discord-alertmanager:
    image: ghcr.io/lrstanley/discord-alertmanager:latest
    restart: unless-stopped
    environment:
      - DRY_RUN=true
      - LOG_LEVEL=debug
      # TODO
